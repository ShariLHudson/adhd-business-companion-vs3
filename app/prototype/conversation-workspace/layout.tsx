/** Full-viewport Conservatory — conversation creates the workspace. */
export default function ConversationWorkspaceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="cw4-layout-root">{children}</div>;
}
