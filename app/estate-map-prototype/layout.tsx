/** Full-viewport shell for Estate Map prototype. */
export default function EstateMapPrototypeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="em-layout-root">{children}</div>;
}
