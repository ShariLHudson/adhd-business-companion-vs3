import { pricingDomainAsStrategyType } from "../domains/pricing";

/** Pricing Strategy — Phase 4A domain pack bridged into the shared registry. */
export const pricingStrategyType = pricingDomainAsStrategyType();
