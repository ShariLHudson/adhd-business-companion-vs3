import { growthDomainAsStrategyType } from "../domains/growth";

/** Growth Strategy — Phase 4C domain pack bridged into the shared registry. */
export const growthStrategyType = growthDomainAsStrategyType();
