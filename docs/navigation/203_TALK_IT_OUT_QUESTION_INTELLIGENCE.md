# 203 — Question Intelligence

**Runtime:** `lib/talkItOut/questionIntelligence.ts`

Blocks stacked, abstract, already-answered, hidden-meaning, and generic probes. Candidate banks are filtered when context is concrete. Failed questions regenerate via `groundedQuestionFallback`.
