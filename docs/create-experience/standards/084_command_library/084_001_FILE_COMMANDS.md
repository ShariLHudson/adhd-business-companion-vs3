# File Commands

- Create New, Open, Recent, Rename, Duplicate, Branch, Move, Archive, Restore, Move to Trash, Permanently Delete, Save as Template, and Close.
- Create New must create a new Work ID without overwriting current work.
- Delete must be recoverable by default; permanent deletion requires explicit confirmation and policy compliance.
