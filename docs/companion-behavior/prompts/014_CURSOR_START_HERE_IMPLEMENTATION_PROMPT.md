# Cursor Start Here — Spark Estate™ Behavior Foundation

Add the attached Companion Behavior Foundation files to the repository as governing product and engineering standards.

## First Action

Do not implement new behavior yet.

Perform a repository placement audit and recommend the correct permanent location for:

- governing standards
- behavior contracts
- regression standards
- implementation reports

Do not duplicate existing standards.

## Then

1. Index all included files.
2. Identify existing documents that conflict, duplicate, or partially overlap.
3. Keep the new success and behavior standards authoritative.
4. Do not delete older files without reporting them.
5. Create a behavior-contract manifest containing:
   - contract ID
   - title
   - status
   - current implementation owner
   - audit status
   - test status
   - live verification status
6. Begin with `007_NEW_CHAT_AND_CONTEXT_ISOLATION_BEHAVIOR_CONTRACT.md`.
7. Audit it using `003_REVERSE_ENGINEERING_AND_IMPLEMENTATION_METHOD.md`.
8. Return the gap map before changing code.

## Important

The current Phase A preview verification remains separate.

Do not deploy production.

Do not begin unrelated feature work.

Do not treat these files as optional guidance. They define intended platform behavior.
